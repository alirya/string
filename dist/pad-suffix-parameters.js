import { padEnd } from "lodash";
/**
 * @see padEnd
 */
export default function PadSuffixParameters(string, length, suffix) {
    return padEnd(string, length, suffix);
}
//# sourceMappingURL=pad-suffix-parameters.js.map