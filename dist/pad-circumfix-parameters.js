import { pad } from "lodash";
export default function PadCircumfixParameters(value, length, circumfix) {
    return pad(value, length, circumfix);
}
//# sourceMappingURL=pad-circumfix-parameters.js.map