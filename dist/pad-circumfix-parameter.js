import { pad } from "lodash";
export default function PadCircumfixObject({ value, length, circumfix }) {
    return pad(value, length, circumfix);
}
//# sourceMappingURL=pad-circumfix-parameter.js.map