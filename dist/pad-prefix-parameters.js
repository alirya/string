import { padStart } from "lodash";
/**
 * @see padStart
 */
export default function PadPrefixParameters(value, length, prefix) {
    return padStart(value, length, prefix);
}
//# sourceMappingURL=pad-prefix-parameters.js.map