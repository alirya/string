import { MaximumArgument } from "../string/maximum-parameter";
export { MaximumArgument };
export default function MaximumParameter({ value, maximum, inclusive, subject, criteria, converter }: MaximumArgument): Error;
